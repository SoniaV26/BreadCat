{{#if user}}
<div id="wrapper">
    <h1>{{this.name}}</h1>
    <p>{{this.email}}</p>
    {{#each restrictions}}
        <p>{{this.restriction}}</p>
    {{/each}}
    <ul>
    {{#each messages}}
        <p>{{this.message}}</p>
    {{/each}}
    </ul>
</div>
{{/if}}
<h1>Title</h1>